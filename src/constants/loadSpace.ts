type LoadSpaceTypes = {
  loadSpaceId: string;
  width: number;
  height: number;
  length: number;
};

export const LOADSPACE: LoadSpaceTypes = {
  loadSpaceId: "0",
  width: 100,
  height: 20,
  length: 25,
};
