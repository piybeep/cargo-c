type LoadSpaceTypes = {
  loadSpaceId: string;
  width: number;
  height: number;
  length: number;
};

export const LOADSPACE: LoadSpaceTypes = {
  loadSpaceId: "0",
  width: 50,
  height: 20,
  length: 25,
};
